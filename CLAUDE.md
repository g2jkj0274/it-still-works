# 프로젝트 규칙

아이소메트릭 복셀 생존 샌드박스. 회로 장치 제작이 코어 시스템.
부품 명세·프로토타입 범위·개념 대응표는 `docs/spec.md` 참조.

## 아키텍처 — 위반 금지

- 시뮬레이션 레이어는 노드 트리/렌더링에 의존하지 않는다. 헤드리스 단독 실행 가능해야 함
- 표현 레이어는 시뮬레이션을 읽기만 한다. 역방향 의존 금지
- 시뮬레이션 로직에 부동소수점 사용 금지. 정수 격자만 사용
- 난수는 시드 고정된 단일 RNG 인스턴스만 사용. `randi()` / `randf()` 전역 호출 금지
- 딕셔너리 등 순서 비보장 자료구조를 시뮬레이션 판단 근거로 사용 금지
- 모든 상태 변경은 명령 객체를 경유한다. 입력이 월드 상태를 직접 수정 금지
- 시뮬레이션은 고정 틱 루프에서만 진행. `_process`에서 갱신 금지

## 테스트

- 테스트를 수정해서 통과시키지 말 것. 구현을 고칠 것
- 새 부품 추가 시 유닛 테스트와 결정론 회귀 테스트를 함께 추가
- 결정론 회귀 테스트가 깨지면 다른 모든 작업보다 우선해 수정

```bash
# 전체 테스트
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a test -c --ignoreHeadlessMode

# 헤드리스 실행 확인
godot --headless --path . --quit
```

## GDScript

- 파이썬 관용구를 적용하지 말 것 (리스트 컴프리헨션, `with`, 데코레이터 등)
- 존재를 확신할 수 없는 Godot API는 사용 전 문서 확인. 추측 금지
- Godot 4.x 기준. 3.x 문법 사용 금지

## 게임 설계

- 회로 오류의 원인을 알려주는 UI를 만들지 말 것. 동작만 보여준다
- 부품을 임의로 추가하지 말 것. 부품 목록 변경은 `docs/spec.md`에서만
- 게임 화면에 프로그래밍 용어를 노출하지 말 것 (변수, 함수, 조건문 등)

## 작업 방식

- 기능 단위로 브랜치를 만들고, 반복마다 커밋
- 스펙과 구현이 어긋나면 `docs/spec.md`를 함께 갱신할 것
