# it-still-works — 프로젝트 헌법

이 파일은 모든 세션·서브에이전트가 가장 먼저 읽는다. 여기 적힌 "고정 원칙"은 변경 금지다.
바꾸고 싶은 이유가 생기면 코드를 바꾸지 말고 `docs/BLOCKERS.md`에 적고 다른 작업으로 넘어간다.

## 한 줄 정의
아이소메트릭 생존 샌드박스. 생존이 목적, 회로는 수단.
게임의 주 시스템은 "기계"가 아니라 "기계가 생겨나는 규칙"이다.
설계자는 부품과 세계 규칙만 만들고, 기계는 전부 플레이어가 조합으로 만든다.

## 고정 원칙 (변경 금지)

### P1. 기계는 없다. 부품만 있다.
- 부품 하나는 한 가지 동작만 한다: 감지 / 전달 / 논리 / 작동 중 하나.
- 크래프팅 레시피에 완성된 기계(자동 문, 함정, 분류기, 드릴, 컨베이어 라인)를 넣지 않는다. 레시피는 부품만 만든다.
- 새 부품은 "설계 의도 외의 쓰임 3가지"를 `docs/PRIMITIVES.md`에 적을 수 있어야 추가된다. 3개가 안 나오면 부품이 아니라 기계다 → 재설계.

### P2. 속성 기반 상호작용. 특정 블록 쌍 스크립트 금지.
- 모든 블록/엔티티는 속성 집합으로 정의된다: `solid`, `pushable`, `conductive`, `container`, `breakable(내구도)`, `emits_sound`, `gravity`, `flammable`, `liquid` 등.
- 시스템은 속성만 본다. "피스톤이 문을 만나면"이 아니라 "pushable이면 민다".
- `if block.type == "door"` 같은 타입 분기가 시뮬레이션 코드에 등장하면 원칙 위반이다. (렌더링·사운드 코드는 예외)

### P3. 결정론.
- 시뮬레이션은 렌더 프레임과 분리된 고정 틱(20 tps)으로 돈다.
- 갱신 순서는 문서화되고 고정된다 (`docs/SIM_ORDER.md`).
- 같은 시드 + 같은 입력 = 같은 상태. 이건 CI 테스트로 강제된다 (M0에서 구축).
- 랜덤은 시드된 RNG 하나만 쓴다. `randi()` 직접 호출 금지.

### P4. 상태는 보인다. 원인은 알려주지 않는다.
- 신호 on/off, 리피터 잔여 틱, 압력판 눌림, 블록 내구도는 화면에서 읽힌다.
- "이 회로가 왜 안 되는지" 설명하는 UI·툴팁·튜토리얼은 만들지 않는다. 플레이어가 찾는다.

### P5. 실패는 없다.
- 죽으면 마인크래프트식 리스폰. 게임오버·세이브 삭제·패널티 화면 없음.
- 톤은 밝고 경쾌. 공포·고어·붉은 비네팅·화면 흔들림 금지.

### P6. 손으로 끝까지 가능. 다만 갈수록 비효율.
- 어떤 시점에도 회로 없이 생존 가능해야 한다.
- 밤마다 손으로 해야 하는 반복 작업(불 유지, 문 닫기, 함정 재장전, 파손 수리)은 일수에 비례해 늘어난다. 자동화의 필요는 강제가 아니라 누적에서 나온다.

### P7. 세계는 무한 청크. 회로는 청크 규칙을 따른다.
- 절차 생성, 무한 확장, 청크 단위 로드/언로드.
- 언로드된 청크의 시뮬레이션은 멈춘다. 신호 상태·리피터 잔여 틱·내구도는 저장되고 복원된다.
- 로드된 청크끼리는 청크 경계를 넘어 신호가 전달된다. 경계에서 끊기는 회로는 버그다.
- 플레이어 주변 로드 반경은 상수 하나로 정의되고 문서화된다.

### P8. 시점과 층.
- 아이소메트릭. 회로는 2D 그리드 위에 놓인다.
- 수직 복잡도는 층(지하/지상/2층) 전환으로 푼다. 한 화면에 한 층.
- 층 사이 신호 전달 부품은 힘 단계(M4) 이후에만 고려한다.

### P9. 생존 수치는 체력 + 허기. 그 이상 추가 금지.
- 온도·수분·정신력 등 추가 수치 금지. 압박은 밤과 허기에서만 온다.

### P10. 밤 몹 규칙.
- 밤에만 등장. 낮이 되면 사라진다(불타지 않음, 그냥 귀가).
- 소리·진동에 반응한다: `emits_sound` 속성이 있는 블록/이벤트(디스펜서 발사, 피스톤 작동, 플레이어 채굴)를 향해 이동한다. 플레이어 추적은 소리보다 우선순위가 낮다.
- 블록을 부순다: `breakable` 블록에 닿으면 내구도를 깎는다. 내구도 0이면 블록 파괴. 방어 장치는 유지보수가 필요하다.
- 몹 행동은 위 규칙의 조합으로만 정의한다. "몹이 X 블록을 특별히 노린다" 같은 예외 금지. 소리 나는 곳으로 가서, 막히면 부순다. 그게 전부.
- 외형은 위협적이지 않게. 젤리·풍선·과일 같은 실루엣.

### P11. 명시적 목표는 없다. 진행 장치는 연구 트리(팩토리오식)뿐이다.
- 퀘스트·도전과제·엔딩 없음. 순수 샌드박스.
- 연구 트리는 UI가 있는 명시적 트리다. 연구대(부품)에 채집 자원을 투입해 노드를 연다.
- 연구 노드가 여는 것은 "부품"과 "세계 규칙"이다. 절대 완성된 기계를 주지 않는다 (P1). 노드 이름에 "자동", "장치", "시스템"이 들어가면 위반이다.
- 4단계(아래 테크트리)는 고정. 단계 안의 노드 구성·비용은 에이전트가 정하되 DECISIONS.md에 기록한다.
- 연구 비용은 채집·조리 자원만 쓴다. 회로로 만들어야만 얻는 자원은 금지 (P6).
- 연구 완료는 축하 연출 없이 조용히 열린다. 연구는 목표가 아니라 도구다.

### P12. 아트는 코드로 생성한 픽셀아트.
- 에셋은 스크립트(`tools/pixelart/`)로 생성되어 저장소에 커밋된다. 손그림·외부 다운로드 금지.
- 팔레트는 밝고 채도 높은 메이플스토리2 톤. 단, 실존 게임의 캐릭터·몬스터·로고·UI를 재현하지 않는다. 오마주는 색감과 비율까지다.
- 플레이스홀더(단색 사각형)는 M6까지 허용, M7에서 전량 교체.

## 테크트리 (연구 트리의 4단계, 순서 고정)
1. 신호 — 전선, 스위치, 압력판, 소리 감지기, 리피터(지연), NOT, 램프, 디스펜서
2. 힘 — 피스톤, 끈끈이 피스톤, 블록 감지기, `pushable` 규칙
3. 아이템 운반 — 호퍼, 컨베이어, `container` 규칙, 아이템 필터(속성 기반)
4. 액체 — 물, 용암, 셀룰러 오토마타 흐름, 액체가 아이템·몹을 민다

## 기술 스택 (고정)
- Godot 4.x (프로젝트의 `project.godot` 버전을 따른다), GDScript
- GdUnit4 — 모든 시뮬레이션 로직은 헤드리스 테스트 가능해야 한다
- 시뮬레이션 코드(`sim/`)는 Node 트리에 의존하지 않는 순수 GDScript 클래스로 작성한다. 렌더링(`view/`)이 sim을 읽는다. 반대 방향 의존 금지.

## 문서 (에이전트가 유지하는 기억)
- `docs/STATE.md` — 현재 마일스톤, 진행 중 태스크, 마지막으로 통과한 테스트. 매 이터레이션 갱신.
- `docs/DECISIONS.md` — 헌법에 없는 결정을 내렸을 때 기록. 형식: 날짜 / 결정 / 근거 / 대안. 이후 세션은 이 결정을 뒤집지 않는다.
- `docs/PRIMITIVES.md` — 부품 목록. 각 부품: 속성, 의도된 쓰임 1개, 의도 외 쓰임 3개 이상.
- `docs/SIM_ORDER.md` — 틱 갱신 순서.
- `docs/EMERGENCE_LOG.md` — 창발 테스트 결과. 발견된 비의도 기계 목록.
- `docs/BLOCKERS.md` — 헌법과 충돌해서 진행 못 하는 항목. 사람이 읽는 유일한 파일.
- `docs/JOURNAL.md` — 이터레이션마다 한 줄.

## 절대 규칙
- 사용자에게 질문하지 않는다. 결정이 필요하면 헌법 안에서 결정하고 `DECISIONS.md`에 적는다.
- 헌법과 충돌하면 `BLOCKERS.md`에 적고 다른 태스크로 간다. 헌법을 고치지 않는다.
- 테스트 없는 시뮬레이션 코드는 커밋하지 않는다.
- 게임 개발 스킬(`gamedev-skills` 라우터 + godot 번들)이 설치되어 있으면 Godot 관련 태스크에서 라우터를 통해 사용한다. 단, 스킬의 조언이 이 헌법과 충돌하면 헌법이 이긴다.
- 커밋은 작게, 자주. 형식: `type(scope): 한 줄` (feat/fix/test/refactor/art/docs).
- 원칙 위반을 발견하면 새 기능보다 먼저 고친다.
