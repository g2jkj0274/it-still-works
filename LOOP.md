# 자율 개발 루프

이 문서는 실행 지시다. 세션을 시작하면 이 문서를 읽고 즉시 루프에 들어간다.
사용자는 없다. 질문할 대상도 없다. 멈춤 조건은 맨 아래 "완성 정의"뿐이다.

## 0. 세션 시작 (매 세션 1회)
1. `CLAUDE.md` 전체를 읽는다.
2. `docs/STATE.md`, `docs/DECISIONS.md`, `docs/BLOCKERS.md`, `docs/JOURNAL.md` 마지막 20줄을 읽는다.
3. `git status`, `git log --oneline -20`으로 저장소 상태를 파악한다. 커밋 안 된 변경이 있으면 테스트 후 커밋하거나 되돌린다.
4. 테스트를 한 번 전부 돌린다. 실패가 있으면 그것이 첫 태스크다.
5. STATE.md가 없으면 M0부터 시작한다.

## 1. 이터레이션 (반복)

### 1.1 태스크 선택
- 현재 마일스톤의 미완료 항목 중 가장 작은 세로 슬라이스를 고른다. "세로"란 sim + view + test가 모두 포함된다는 뜻이다.
- 한 이터레이션의 태스크는 한 커밋 크기(파일 5개 이내, 변경 300줄 이내)로 자른다. 크면 쪼갠다.
- STATE.md에 "진행 중: <태스크>"를 적고 시작한다.

### 1.2 설계 검토 → `architect` 서브에이전트
- 태스크 설명과 영향받는 파일 목록을 넘긴다.
- architect는 헌법 P1~P12 위반 여부와 속성 설계를 검토하고 승인/거절/수정안을 돌려준다.
- 거절이면 수정안대로 태스크를 다시 쓴다. 2회 거절이면 BLOCKERS.md에 적고 다른 태스크로 간다.

### 1.3 구현 → `builder` 서브에이전트
- 승인된 태스크를 넘긴다. builder는 코드 + 단위 테스트를 함께 작성한다.
- sim 코드는 Node 없이 테스트 가능해야 한다.

### 1.4 검증 → `tester` 서브에이전트
- 전체 테스트 스위트 헤드리스 실행.
- 결정론 테스트: 같은 시드로 2000틱 2회 실행, 상태 해시 비교.
- 성능 예산: 로드된 청크 내 회로 부품 1000개에서 틱당 5ms 이하.
- 실패 시 실패 로그를 builder에게 돌려보내 고친다. 같은 태스크 3회 실패 → 접근을 바꾸고 DECISIONS.md에 "포기한 접근"을 기록한다.

### 1.5 커밋
- 테스트 전부 통과 시에만 커밋한다.
- STATE.md 갱신, JOURNAL.md에 한 줄 추가 (`YYYY-MM-DD HH:MM | M? | 태스크 | 결과`).

### 1.6 마일스톤 종료 조건
- 마일스톤의 모든 항목 완료 + 아래 창발 테스트 통과 시에만 다음 마일스톤으로 간다.
- 창발 테스트 → `playtester` 서브에이전트가 `/emergence-test` 스킬로 수행.
- 실패하면 원인은 부품이 아니라 세계 규칙 부족이다. 규칙을 하나 추가하는 태스크를 만들고 (architect 검토 필수) 다시 테스트한다.

### 1.7 주기적 감사 (10 이터레이션마다)
- `architect`에게 저장소 전체 원칙 감사를 시킨다: 타입 분기, 시드 안 된 랜덤, view→sim 역방향 의존, 레시피 안의 기계.
- 위반은 새 기능보다 먼저 고친다.

## 2. 마일스톤

### M0. 정리와 안전망
- 게임 개발 스킬 설치 및 확인:
  `claude plugin marketplace add gamedev-skills/awesome-gamedev-agent-skills`
  `claude plugin install router@awesome-gamedev-agent-skills`
  `claude plugin install godot@awesome-gamedev-agent-skills`
  실패하면 `npx skills add gamedev-skills/awesome-gamedev-agent-skills -a claude-code`로 대체. 둘 다 실패하면 설치 없이 진행하고 BLOCKERS가 아닌 DECISIONS에 기록.
- 기존 저장소 감사. 헌법과 맞는 코드/맞지 않는 코드/판단 보류로 분류해 `docs/AUDIT.md`에 기록.
- 맞지 않는 코드는 삭제하지 말고 `legacy/`로 이동 (빌드에서 제외). 이후 마일스톤에서 재활용 가능한 것만 꺼내 쓴다.
- 헤드리스 테스트 명령 확정 (`tools/test.sh` 또는 `.cmd`). GdUnit4 실행 경로를 확인하고 문서화.
- 결정론 테스트 하네스, 시드 RNG 싱글턴, 고정 틱 루프, `sim/`·`view/` 분리 골격.
- 완료 조건: 빈 세계에서 2000틱 결정론 테스트 통과.

### M1. 세계
- 청크 절차 생성(시드 기반), 로드/언로드, 저장/불러오기.
- 아이소메트릭 렌더, 층 전환, 플레이어 이동.
- 채집(블록 캐기)·배치, 인벤토리, 기초 크래프팅(부품만).
- 연구대 부품과 연구 트리 UI 골격(P11). M1에서는 노드 없이 빈 트리, M3부터 단계별 노드 추가.
- 블록 속성 시스템(P2)과 속성 테이블 `data/blocks.json`.
- 완료 조건: 걸어서 새 청크 생성 → 저장 → 종료 → 로드 → 동일 상태 테스트 통과.

### M2. 생존
- 낮/밤 주기, 체력, 허기(음식 채집·조리는 부품 아님, 허용).
- 리스폰. 밤 몹: 소리 감지, `breakable` 파괴, 낮 소멸.
- 손 작업 누적 규칙(P6): 밤마다 필요한 수리·보급량이 일수에 비례해 증가.
- 완료 조건: 회로 없이 10일 생존 가능(스크립트 플레이어)하되 10일차 수동 작업량이 1일차의 5배 이상임을 테스트로 증명.

### M3. 신호 (테크트리 1)
- 부품 8개: 전선, 스위치, 압력판, 소리 감지기, 리피터, NOT, 램프, 디스펜서.
- 세계 규칙 3개 활성: 중력(`gravity` 블록 낙하), 아이템 엔티티(떨어진 아이템은 물리 객체), 몹 규칙(P10).
- 청크 경계 신호 전달, 언로드 시 상태 보존.
- 완료 조건: 창발 테스트 1 통과.

### M4. 힘 (테크트리 2)
- 피스톤, 끈끈이 피스톤, 블록 감지기. `pushable` 규칙, 밀림 체인 한계(상수).
- 층 간 신호 부품 검토 (P8).
- 완료 조건: 창발 테스트 2 통과.

### M5. 아이템 운반 (테크트리 3)
- 호퍼, 컨베이어, `container` 규칙, 속성 기반 필터.
- 완료 조건: 창발 테스트 3 통과.

### M6. 액체 (테크트리 4)
- 물·용암 셀룰러 오토마타, 액체가 `pushable` 아이템·몹을 민다, 용암은 `flammable` 파괴.
- 완료 조건: 창발 테스트 4 통과 + 성능 예산 유지.

### M7. 완성
- `pixel-artist`로 전량 픽셀아트 교체. 플레이스홀더 0개.
- 60fps, 회로 부품 1000개, 몹 50마리 동시.
- 30분 무인 자동 플레이(스크립트 플레이어) 크래시 0.
- Windows 익스포트 빌드 생성, `dist/`에 커밋 (또는 릴리스 태그).
- 튜토리얼 없음. 대신 첫 3일 밤에 발견형 힌트(예: 스타트 지점 근처에 켜진 램프+전선 하나)만 둔다.

## 3. 완성 정의 (루프 종료 조건)
아래 전부를 만족하면 `docs/STATE.md`에 `DONE`을 적고 루프를 멈춘다. 하나라도 빠지면 계속한다.
1. M0~M7 모든 완료 조건 통과.
2. `docs/EMERGENCE_LOG.md`에 설계 문서에 없던 기계가 12개 이상 기록됨 (마일스톤당 3개).
3. 전체 테스트 통과, 결정론 테스트 통과.
4. `docs/BLOCKERS.md`가 비어 있음. (비어 있지 않으면 DONE이 아니라 `BLOCKED`를 적고 멈춘다 — 이때만 사람이 개입한다.)
5. 익스포트 빌드가 존재하고 실행된다.

## 4. 금지
- 질문하기. 확인 요청하기. "어떻게 할까요?"로 끝나는 출력.
- 헌법 수정.
- 테스트 스킵·삭제로 통과시키기.
- 마일스톤 건너뛰기.
- "나중에 고침" 주석. TODO는 STATE.md에 태스크로 적는다.
