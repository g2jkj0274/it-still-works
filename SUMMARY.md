# 야간 자율 작업 기록

마지막 갱신: 3단계(되풀이) 완료 시점

## 진행 상황

| 단계 | 상태 |
|---|---|
| 0. 시각 자가검증 | ✅ 완료 (지침대로 뒤로 미루는 대신, 이미 동작해서 먼저 머지함) |
| 1. 조작감 | ✅ 완료 |
| 2. 감지기·작동기 → 자동문 | ✅ 완료 |
| 3. 되풀이 | ✅ 완료 |
| 4. 상자 | 진행 예정 |
| 5~6. 갈림길 / 생존 | 미착수 |
| 7. 비주얼 톤 | 미착수 |

## 완료한 단계

### 0. feat/screenshot-verification — 시각 자가검증

`tools/screenshot_runner.gd` — 게임을 실제로 띄워 단계별 스크린샷을 남기고
명백한 이상을 찾는다.

```bash
godot --path . -s res://tools/screenshot_runner.gd
```

`reports/screenshots/` 에 PNG 와 `REPORT.txt` 저장. 이상이 있으면 종료 코드 1.

감지 항목: 검은 화면 / 단색 화면 / 캐릭터 미표시.

### 1. feat/game-feel — 조작감

**서브유닛 이동** — 한 칸을 1000으로 나눈 정수 좌표. 걸음 250, 낙하 500
서브유닛/틱이라 칸 경계에 정확히 선다. 실수를 쓰지 않아 결정론 유지.
명령은 목표만 정하고 칸을 건너는 일은 틱이 진행한다.

**마우스 겨냥** — Amanatides-Woo 격자 훑기로 시선이 닿는 첫 블록과 맞은 면을
찾는다. 새 블록은 그 면 바깥에 놓인다. 사거리 5칸. 겨냥한 곳이 없으면
바라보는 앞 칸으로 물러나므로 키만으로도 놀 수 있다. 강조 표시는 어느 칸에
손이 닿는지만 보여주고 이유는 말하지 않는다.

**부수기** — 지시하신 "아래 한 칸 제한"은 코드에 없었다. 실제 제약은
`facing_cell()` 하나만 겨냥할 수 있었던 것이고, 마우스 겨냥이 그것을 없앴다.
블록 종류 제한도 원래 없었다. 지반층(z=0)만 못 부순다.

**재료와 핫바** — 부수면 재료가 들어오고 놓으면 쓴다. 핫바에 흙·돌·나무의
개수와 고른 칸이 보인다.

테스트 283개 통과. 스크린샷 6단계 이상 0건.

### 2. feat/circuit-detector-actuator — 감지기·작동기·자동문

**신호** — 값을 가진다. 불리언/정수/실수. 실수는 1000배 고정소수점이라
시뮬레이션에 부동소수점이 없다. 3.7 → 3700, 정수로 읽으면 3.

**전파** — 계산과 반영을 갈라 1틱에 한 부품씩 나아간다. 감지기가 본 것이
문에 닿기까지 두 틱.

**감지기** — 대상은 놓을 때 정한다(T로 고름). 지금 동작하는 것은 사람(3칸
근접)과 물건(개수). 위협·밤·작물은 아직 세상에 없어 거짓이 아니라 신호 없음을
낸다.

**작동기** — 맞닿은 문을 연다/닫는다. 사람이 서 있는 칸에서는 닫지 않는다.

**자동문이 실제로 돈다.** 손으로 심어 둔 것뿐 아니라 플레이어가 명령으로
짓는 경로까지 테스트로 확인했다. 스크린샷 7단계에도 넣었다.

조작: 5 눈 / 6 손 / R 두 번(잇기) / T(눈이 볼 것).

테스트 339개 통과. 스크린샷 7단계 이상 0건.

### 3. feat/circuit-repeater — 되풀이

횟수(for) / 조건(while) / 무한. 받은 값을 정한 간격으로 되풀이해 내보낸다.
켜짐/꺼짐이 아니라 값을 되풀이한다.

**과열** — 40번 내보내면 정지하고, 타 버린 부품은 부숴도 재료가 돌아오지
않는다. 쉬는 동안 식으므로 정해진 만큼만 도는 되풀이는 아무리 여러 번 써도
타지 않는다. 끝없는 되풀이만 탄다.

**신호가 끊겼다 들어올 때 새로 시작한다.** 처음엔 이 조건을 빼먹어 횟수
지정이 무의미해졌다(신호가 참인 채로 머물면 계속 다시 시작). 테스트가 잡았다.

부품 설정값을 정수 목록으로 일반화했다. 상자와 갈림길도 이 자리를 쓴다.

조작: 7 되풀이 / T로 설정 넘기기(3번·10번·조건·무한).

테스트 352개 통과. 스크린샷 7단계 이상 0건.

## 스펙에 없어 스스로 내린 설계 판단

되돌리기 쉬운 것들만 정했다. 모두 상수 하나 또는 함수 하나로 바꿀 수 있다.

1. **빈손으로 시작한다** (`Inventory` 초기값 0). 첫 블록은 손으로 부숴 얻는다.
   스펙 §6 "손으로도 되지만 힘듦"에 맞췄다. 초반이 답답하면 시작 재료를 주는
   쪽이 나을 수 있다.
2. **걸음 속도 250 서브유닛/틱** = 초당 5칸. 1000을 나누어떨어뜨려야 칸 경계에
   정확히 서므로 250/200/125 중에서 골라야 한다.
3. **사거리 5칸** (`BlockTarget.REACH_CELLS`).
4. **걷는 중에는 다음 걸음 명령을 무시한다.** 칸 경계에 정확히 서야 다음 판정이
   맞기 때문이다. 연달아 누르면 살짝 끊기는 느낌이 날 수 있다.
5. **실수 신호를 1000배 고정소수점으로 담는다.** 스펙은 값 타입에 실수를
   두는데 CLAUDE.md 는 시뮬레이션의 부동소수점을 금지한다. 고정소수점이 두
   요구를 모두 지키는 유일한 길이라 판단했다. 스펙 §4.1 에 적었다.
   결과: 실수의 정밀도가 1/1000 로 고정된다.
6. **감지기 근접 거리 3칸** (`DetectorPart.SENSE_RADIUS`).
7. **작동기는 맞닿은 여섯 칸의 문을 모두 작동시킨다.** 스펙은 "붙어 있는
   블록"이라고만 한다. 한 칸만 고르게 하려면 부품에 방향을 줘야 하는데,
   프로토타입에서는 너그러운 쪽이 낫다고 봤다.
8. **아직 없는 감지 대상은 거짓이 아니라 신호 없음.** 거짓을 내면 "없다"와
   "아직 만들지 않았다"가 구별되지 않는다.
9. **과열 한계 40회, 쉬는 동안 1틱에 1씩 식음** (`RepeaterPart.OVERHEAT_LIMIT`).
   스펙은 "무한 반복은 과열되어 정지"라고만 한다. 정해진 횟수만 도는
   되풀이까지 타면 안 되므로 식는 규칙을 넣었다.
10. **타 버린 되풀이는 부숴도 재료가 안 돌아온다.** 스펙의 "소모된 자원은
    돌아오지 않는다"를 이렇게 새겼다. 다른 해석도 가능하다 — 예컨대 되풀이가
    돌면서 자원을 계속 먹게 할 수도 있다.
11. **되풀이 설정은 미리 정한 네 가지 중에서 고른다.** 숫자를 자유롭게 넣을
    화면이 아직 없다. 부품 자체는 임의의 횟수·간격을 받는다.
12. **스크린샷은 창을 띄워 찍는다.** `--headless` 는 렌더링 드라이버가 더미라
   캡처가 안 된다. 확인한 사실이며 우회로가 없다.

## 건너뛴 것

없음.

## 아침에 확인할 것 (우선순위 순)

1. **직접 플레이해서 조작감을 볼 것.** `godot --path .` 로 실행.
   WASD 이동 / 마우스 겨냥 / E 놓기 / Q 부수기 / 1~6 재료 / R 두 번 잇기 / T 눈이 볼 것.
   스펙 §8 은 여기를 판정 지점으로 잡는다 — *"1번이 재미없으면 부품을 늘려도
   소용없다."* 위 판단 2·4번(걸음 속도, 걷는 중 입력 무시)이 특히 손에 걸린다.
2. **되풀이 과열을 손해로 느끼는지.** 판단 10번이 특히 임의적이다.
   무한 되풀이를 만들어 놓고 20초 기다리면 부품이 탄다.
3. **자동문을 손으로 지어 볼 것.** 문 → 손 → 눈 순으로 놓고 눈과 손을 R로
   잇는다. 스펙 §8 의 4단계 판정이 여기다. 배선을 R 두 번으로 잇는 조작이
   가장 의심스럽다 — 만들면서 답답한지 봐 달라.
4. **빈손 시작이 맞는지.** 첫 1분이 지루하면 시작 재료를 주는 쪽으로.
5. `reports/screenshots/*.png` 를 눈으로 볼 것. 도구는 "아무것도 안 나왔다"만
   판정한다. 보기 좋은지는 판정하지 않는다.
