# 야간 자율 작업 기록

마지막 갱신: 인벤토리·궤짝(feat/real-inventory) 완료 시점

## 진행 상황

| 단계 | 상태 |
|---|---|
| 0. 시각 자가검증 | ✅ 완료 |
| 1. 조작감 | ✅ 완료 |
| 2. 감지기·작동기 → 자동문 | ✅ 완료 |
| 3. 되풀이 | ✅ 완료 |
| 4. 상자 | ✅ 완료 |
| 5. 갈림길 | ✅ 완료 |
| 6. 생존 (낮밤·지표·위협·작물) | ✅ 완료 |
| 7. 비주얼 톤 | ✅ 완료 (Kenney CC0 에셋 포함) |
| 8. 묶음 (스펙 §4.3) | ✅ 완료 |
| 9. 제작법 (스펙 §3.6) | ✅ 완료 |
| 10. 저장/불러오기 (스펙 §6) | ✅ 완료 |

**테스트 819개 통과. 스크린샷 17단계 이상 0건.**

**스펙 §6 의 프로토타입 범위가 다 찼다.** 모든 단계가 `main` 에 머지되어
GitHub 에 올라가 있다.

지시하신 순서와 다르게 0번(스크린샷)을 맨 앞에서 끝냈다. 이미 동작하는 상태였고,
이후 모든 단계에서 시각 확인에 쓸 수 있어 미루는 것보다 이득이라 판단했다.

---

## 단계별 요약

### 0. 시각 자가검증

```bash
godot --path . -s res://tools/screenshot_runner.gd
```

게임을 실제로 띄워 단계별 스크린샷을 `reports/screenshots/` 에 남기고 검은 화면 ·
단색 화면 · 캐릭터 미표시를 감지한다. 이상이 있으면 종료 코드 1.

캐릭터 표시 여부는 색이 아니라 **캐릭터를 켠 화면과 끈 화면의 차분**으로 본다.
7단계에서 팔레트를 통째로 바꿨는데도 이 판정은 그대로 살아남았다.

### 1. 조작감

- **서브유닛 이동** — 한 칸을 1000으로 나눈 정수 좌표. 걸음 250, 낙하 500/틱.
  실수를 쓰지 않아 결정론 유지.
- **마우스 겨냥** — Amanatides-Woo 격자 훑기. 맞은 면 바깥에 블록이 놓인다.
  사거리 5칸. 겨냥한 곳이 없으면 앞 칸으로 물러나 키만으로도 놀 수 있다.
- **부수기** — 지시하신 "아래 한 칸 제한"은 코드에 없었다. 실제 제약은
  앞 칸 하나만 겨냥할 수 있었던 것이고 마우스 겨냥이 없앴다.
- **재료와 핫바** — 부수면 들어오고 놓으면 쓴다.

### 2. 감지기 · 작동기 · 자동문

신호는 값을 가진다. 실수는 1000배 고정소수점이라 시뮬레이션에 부동소수점이 없다.
계산과 반영을 갈라 **1틱에 한 부품씩** 나아간다.

### 3. 되풀이

횟수 / 조건 / 무한. 40번 내보내면 과열되어 정지하고 **타 버린 부품은 부숴도
재료가 돌아오지 않는다.** 쉬는 동안 식으므로 정해진 만큼만 도는 되풀이는 타지 않는다.

### 4. 상자

네모(정수) / 둥근(실수) / 작은(불리언). 3.7 → 네모 → 3, 되돌려도 3.7 은 돌아오지
않는다. 담긴 값은 늘 밖으로 나가고 있어 언제든 읽힌다.

### 5. 갈림길

참/거짓 두 출구. 값은 그대로 흘려보내고 어느 쪽으로 보낼지만 정한다.
비교(`>=`, `<`, `==`)와 AND/OR. **흐르지 않는 배선은 흐린 색으로 보인다.**

이 단계에서 신호 규약을 고쳤다. 감지기가 조건을 만족하지 않을 때 거짓을 내보내고
있었는데, 그러면 갈림길의 거짓 쪽에 이어진 것이 영영 실행되지 못한다.

**부품 다섯 종이 모두 필요한 장치가 돈다.** 감지기 → 갈림길 → 되풀이 → 상자 →
작동기 → 문. 되풀이가 띄엄띄엄 내보내는 사이를 상자가 메운다. 하나라도 빼면
서지 않는 것까지 테스트했다. 스펙 §5 의 기준점이다.

### 6. 생존

- **낮밤** — 10분 주기(낮 7분/밤 3분). 틱만으로 정해져 따로 든 상태가 없다.
- **지표** — 체력·포만도 각 20. 쓰러지면 인벤토리 절반을 잃고 시작 자리에서
  일어난다. 건축물과 회로는 남는다.
- **위협** — 밤에 넷. 사람보다 느리고, 길이 막히면 앞을 부순다. 자리 고르기에만
  난수를 쓰고 움직임에는 쓰지 않는다.
- **밭과 작물** — 90초면 다 자란다. **거두는 것은 작동기뿐이다.** 손으로는 거두지
  못하므로 자동 농장을 만들 이유가 생긴다. 심고 → 자라고 → 거두고 → 먹는다.

감지기 다섯 대상이 모두 실제로 동작한다.

### 7. 비주얼 톤

색을 `view/palette.gd` 한 곳에 모으고 **명도 0.60 이상, 채도 0.45 이하**를
테스트로 못박았다. 파스텔 폭이 좁아 색이 겹치는 일이 두 번 있었고 두 번 다
테스트가 잡았다. 칸마다 명도를 ±0.045 달리해 넓은 지면의 단조로움을 덜었다
(화면 색 292종 → 540종). 캐릭터는 치비 비율, 그림자는 부드럽게.

---

## 플레이 피드백 반영 (fix/playability)

| 요청 | 결과 |
|---|---|
| 1. WASD 화면 기준 | ⚠ 구조는 바꿨으나 **아이소메트릭에서는 완전히는 불가** — 아래 참조 |
| 2. 부품 지급 | ✅ 상수 하나로 토글 |
| 3. 핫바 개선 | ✅ 숫자·이름·개수, 고른 칸 강조 |
| 4. 용어 정리 | ✅ 오타 포함 |
| 5. 안내 토글 | ✅ 기본 숨김, H / F1 |
| 6. 카메라 | ✅ 26 → 15, 휠 확대, `[` `]` 회전 |
| 7. 부품 설명 한 줄 | ✅ |

### 1번은 절반만 됩니다 — 읽어 주세요

요청대로 입력 회전을 `view/screen_directions.gd` 에 넣고 카메라에서 직접 뽑게
했습니다. 시뮬레이션의 격자 이동 규칙은 건드리지 않았습니다.

**그런데 W 가 화면 정확히 위로 가게 만들 수는 없습니다.** 실제로 재어 봤습니다:

```
north(0,-1) -> 화면 right=+17.62 up=+10.17
east (+1,0) -> 화면 right=+17.62 up=-10.17
```

네 격자 방향이 화면에서 30° / 150° / 210° / 330° 에 놓이고, **가로 성분이 세로보다
큽니다.** 어느 방향을 W 에 물려도 위보다 옆으로 더 갑니다. 이건 매핑 문제가 아니라
아이소메트릭 투영의 성질입니다. 요가 0°(또는 90° 배수)일 때만 격자축이 화면축과
나란해지는데, 그러면 다이아몬드 모양이 사라집니다.

그래서 **시점을 45°씩 돌릴 수 있게** 했습니다. `[` `]` 로 두 번 돌리면 요가 0°가
되어 W 가 정확히 위로 갑니다. 조작은 시점을 따라가므로 어느 쪽이든 맞습니다.
어느 쪽을 기본으로 할지는 봐 주셔야 합니다 (`IsometricCamera.YAW_DEGREES`).

지금 지키는 것은 "정확히 위"가 아니라 **누른 쪽으로는 간다**입니다. W 는 적어도
위로, D 는 적어도 오른쪽으로 갑니다. 테스트가 이 성질을 못박습니다.

### 완료 조건

테스트 496개 통과. 스크린샷 8단계 이상 0건.

`test_a_player_can_build_an_automatic_door` — 사람이 손에 든 것만으로 문 → 작동기 →
감지기를 놓고 R 두 번으로 이어 문이 열리는 것까지 **입력 레이어를 거쳐** 확인합니다.

---

## UI·이동 수정 (fix/ui-and-movement)

| 요청 | 결과 |
|---|---|
| 1. UI 잘림 | ✅ 매 프레임 화면 크기에서 다시 잼. 좁으면 칸을 줄여서라도 넣음 |
| 2. 8방향 이동 | ✅ **W 가 화면 정확히 위로 간다** |
| 3. 카메라 45도 유지 | ✅ |
| 4. 갈림길 출구 선택 | ✅ T 전환 + 화면 표시 + 배선 색 구분 |

### 8방향이 지난번 문제를 풀었습니다

지난번에 "아이소메트릭에서는 W 가 화면 위로 갈 수 없다"고 보고했는데,
**대각선을 더하니 풀립니다.** 요 45도에서 네 대각선이 화면의 위아래좌우와
정확히 맞아떨어집니다. 실측:

```
W(up)    -> grid(-1,-1)  화면 right=+0.0  up=+35.3
D(right) -> grid(+1,-1)  화면 right=+61.1 up=-0.0
```

가로 성분이 0.0 입니다. 카메라 각도는 45도 그대로입니다.

한 가지 정정: W 는 **서+북** 조합입니다 (북+동이 아니라). 북+동은 화면
오른쪽(D)이 됩니다. 어느 쪽이든 카메라에서 뽑으므로 시점을 돌려도 맞습니다.

### 대각선 규칙

- **양옆이 모두 열려 있어야** 지나간다 (모서리 끼임 방지). 세 가지 막힘
  경우를 각각 테스트로 못박았다
- 대각선으로는 턱을 오르지 않는다. 같은 높이로만 건넌다
- 축별 속도를 250 → 200 으로 낮춰 곧은 걸음과 빠르기를 맞췄다.
  1000 을 나누어떨어뜨리므로 칸 경계에 정확히 선다 (부동소수점 없음)

### 갈림길 출구

"갈림길 선택 상태"를 **배선 출발점으로 갈림길을 잡은 상태**로 읽었습니다.
출구는 이을 때에만 뜻이 있고, 그래야 T 가 다른 부품에서 하던 일(설정 바꾸기)과
부딪히지 않습니다. 하단 한 줄에 `잇는 중 — 참 쪽으로 나간다. T 로 바꾸고,
이을 부품에 R` 이 뜹니다. 참 쪽은 노랑, 거짓 쪽은 파랑으로 그려집니다.

### 완료 조건

테스트 524개 통과. 스크린샷 8단계 이상 0건(핫바 전체 표시 검사 포함).

---

## 설명 글 세로줄 수정 (fix/hint-text)

부품 설명이 화면 한가운데 세로줄로 나와 게임 화면을 가렸다.

**원인은 내가 넣은 줄바꿈 설정이다.** `AUTOWRAP_WORD_SMART` 를 켜면 라벨이
"얼마든지 좁아질 수 있다"고 답한다. 그 최소 크기로 칸 폭을 정하니 폭이 0 에
가깝게 잡히고, 글자마다 줄이 바뀌어 세로줄이 됐다.

고친 방법: 줄바꿈을 끄고 **글자 너비를 폰트에서 직접 재서** 칸을 잡는다.
핫바와 같이 매 sync 마다 화면 크기 기준으로 다시 잰다. 화면보다 길면 잘라 낸다.

`PanelContainer` 를 `Panel` 로 바꿨다. 컨테이너는 자식의 최소 크기를 스스로
정하려 들어 내가 정한 폭과 다툰다.

**회귀 테스트가 실제로 그 버그를 잡는지 확인했다.** 버그 형태로 되돌리면
9개 중 6개가 깨지고, 고친 상태에서는 모두 통과한다. 스크린샷 검증에도
"부품 설명이 가로 한 줄이 아니다" 항목을 넣었다.

테스트 533개 통과. 스크린샷 8단계 이상 0건.

---

## 8방향 조합키 수정 (fix/diagonal-input)

키 하나가 화면 위아래좌우(= 격자 대각선)에만 물려 있어 격자 축 방향으로 갈 길이
없었다. 눌린 키를 하나의 화면 방향으로 모아 여덟 쪽 중 하나로 다듬는다.

실측으로 여덟 쪽이 모두 열린 것을 확인했다:

```
W    -> grid(-1,-1)  화면 right= +0.0 up=+35.3   틱당 8.8px
D    -> grid(+1,-1)  화면 right=+61.1 up= -0.0   틱당 15.3px
W+D  -> grid( 0,-1)  화면 right=+30.5 up=+17.6   틱당 8.8px
S+A  -> grid( 0,+1)  화면 right=-30.5 up=-17.6   틱당 8.8px
```

### 모서리 규칙은 그대로 격자 대각선에 둡니다

벽 모서리를 뚫고 가느냐는 **화면이 아니라 격자의 문제**입니다. 시점을 돌려도
대각선은 대각선입니다. 그래서 규칙 자체는 옮기지 않았습니다.

다만 그 대각선이 이제 W/A/S/D 라서 **W 로 턱을 못 오르는** 문제가 생겼습니다.
곧은 걸음과 같이 한 칸 턱은 오르되, 오를 때도 양옆이 열려 있어야 하도록 했습니다.

고치는 중에 **구멍을 하나 만들었다가 잡았습니다.** 옆이 막혀 못 가는 경우가
턱 오르기 경로로 새어 나가 모서리 규칙을 우회했습니다. 갈 자리가 실제로 막혀
있을 때만 오르도록 좁히고 `test_a_blocked_side_never_becomes_a_climb` 를 넣었습니다.

### 속도: 지난번 계산이 틀렸습니다

지난 커밋에서 "대각선이 화면에서 sqrt(3) 배 길다"고 적고 속도를 250 → 200 으로
낮췄는데, **화면 세로 눌림을 대각선 쪽에 적용하지 않은 실수**였습니다. 바로 재면:

```
격자 축 한 칸   = sqrt(1/2 + 1/2 * 1/3) = sqrt(2/3)
격자 대각 한 칸 = sqrt(2) * 1/sqrt(3)   = sqrt(2/3)   ← 같다
```

투영이 정확히 그만큼 눌러 줍니다. 길이가 같으니 시간도 같아야 해서 대각선 속도를
곧은 걸음과 같게 되돌렸습니다. 여덟 쪽 중 여섯이 틱당 8.8px 로 일치합니다.
화면 좌우 두 방향만 세로 눌림을 안 받아 빨라 보이는데, 이건 블록 자체가 가로로
넓은 것과 같은 이치라 어색하지 않습니다.

### 결정론 테스트의 구멍도 메웠습니다

끝 상태만 재는 골든은 **걸음 속도에 둔감**했습니다(명령 사이가 넉넉해 어느
속도로도 제때 도착). 대각선 걸음 도중을 재는 `GOLDEN_MID_HASH` 를 더하고,
속도를 200 으로 되돌려 실제로 깨지는 것까지 확인했습니다.

테스트 553개 통과. 스크린샷 8단계 이상 0건.

---

## 묶음 (feat/bundle)

스펙이 **"이 게임의 정체성"** 이라 못박은 §4.3 이다. 없으면 그냥 레드스톤이다.
부품 5종이 다 되어 여기까지 왔다.

완성한 회로를 골라 압축하면 설계도가 되어 손에 들어오고, 다시 놓으면 격자
**한 칸을 차지하는 부품**이 된다.

### 손으로 하는 법

```
V  묶을 칸 고르기 (다시 누르면 뺀다)
B  고른 칸의 몫 돌리기 — 보통 → 들어오는 자리 → 나가는 자리
G  묶기
N  묶음 손에 쥐기 / 다음 묶음으로
E  놓기
```

고른 칸은 화면에 덧그려지고 몫마다 색이 갈린다. 밝은 지면 위에서 흰 표시는
보이지 않아 톤을 낮춰 잡았다. 핫바에 열한째 칸이 생겼고 어느 묶음을 몇 개
쥐고 있는지 함께 적는다. 숫자 키가 열 개뿐이라 이 칸만 `N` 이다.

### 스펙에 없어 스스로 내린 판단 (묶음)

1. **고른 차례가 곧 드나드는 차례다.** 첫째 배선이 첫째 들어오는 자리에 닿고
   첫째 나가는 자리가 첫째 출구다. 신호가 없는 배선도 자리를 차지한다.
   그러지 않으면 어느 배선이 어디로 가는지가 신호의 있고 없음에 따라 흔들린다.
2. **나가는 자리는 그 부품의 첫 출구다.** 갈림길의 거짓 쪽을 밖으로 내보내려면
   안에서 다른 부품으로 받아 그것을 또 하나의 나가는 자리로 삼아야 한다.
   출구 번호까지 고르게 하려면 화면이 하나 더 필요해 프로토타입에서는 접었다.
3. **안에 든 감지기와 작동기는 묶음이 놓인 칸에서 세상을 만난다.** 묶음은
   한 칸짜리 부품이므로 그 안이 어디에 그려져 있었는지는 세상과 상관이 없다.
   덕분에 자동문을 묶어 문 옆에 놓으면 그대로 돈다. 이것을 위해 부품에
   "세상과 닿는 자리"를 따로 두었다.
4. **묶음이 틱을 더 먹지 않는다.** 신호가 들어온 그 틱에 안쪽 첫 부품까지 닿는다.
   한 부품짜리 묶음은 그 부품과 똑같이 동작한다.
5. **타 버린 부품은 묶지 못한다.** 묶어서 되살릴 수 있으면 과열의 대가가 대가가
   아니게 된다. 안에서 탄 묶음은 부숴도 돌아오지 않는다.
6. **묶는 것은 부수는 것이 아니다.** 삼킨 부품은 재료로 돌아오지 않는다.
7. **묶음은 지급하지 않는다.** 안에 무엇이 들었는지 사람이 정하는 것이 묶음이라
   쥐여 주면 회로를 대신 지어 주는 셈이다. 시작 지급에서 뺐다.
8. **묶음 번호는 만든 차례대로 붙는다.** 설계도는 자기보다 작은 번호만 품을 수
   있으므로 **재귀는 생길 수 없다.** 막는 규칙이 아니라 번호 매기는 방식이
   그것을 보장한다.

### 스코프는 설계도와 알맹이를 갈라 얻었다

설계도는 하나고 굴러가는 회로는 놓인 것마다 새로 짓는다. 같은 묶음을 열 개
놓으면 상자도 열 개다. 밖에서는 묶음 한 칸만 보이고 안쪽 부품은 월드 회로에
아예 등록되지 않는다. 캡슐화가 규칙이 아니라 구조에서 나온다.

### 결정론

블록 종류가 하나 늘고 묶음 설계도 목록이 상태에 들어가 **골든 해시 다섯 개를
갱신**했다. 다섯 다 서로 다른 프로세스 3회 실행에서 같은 값임을 확인했다.

묶음 전용 회귀 테스트를 더했다. 짓고 → 묶고 → 놓고 → 부수는 길을 통째로
못박고, 묶음 둘을 나란히 놓아 안쪽 회로가 여럿일 때의 순회 차례까지 지킨다.

### 완료 조건

테스트 610개 통과. 스크린샷 10단계 이상 0건 (`08_choose`, `09_bundle` 추가).

---

## 낮 노출 수정 (fix/daylight-exposure)

"낮이 너무 밝다"는 지적을 받고 재어 보니 **화면의 74%가 순백(1.00, 1.00, 1.00)
으로 날아가 있었다.** 지면이 종이처럼 보이고, 초록으로 보이던 것은 그림자뿐이었다.
블록 경계도, 팔레트가 칸마다 주는 명도 변주도 함께 사라져 있었다.

### 값은 눈이 아니라 재어서 골랐다

햇빛 세기와 하늘빛을 다섯 단계로 바꿔 가며 실제로 렌더해 보고, 날아간 픽셀
비율과 가장 넓은 색을 찍었다:

```
햇빛 1.10 하늘빛 x1.00 -> 날아감 73.4%  넓은 색 1.00 1.00 1.00   색 544종
햇빛 0.65 하늘빛 x0.70 -> 날아감  0.0%  넓은 색 0.85 1.00 0.80   색 734종  ← 초록만 여전히 참
햇빛 0.42 하늘빛 x0.52 -> 날아감  0.0%  넓은 색 0.62 0.84 0.58   색 757종  ← 흙의 제 색
햇빛 0.34 하늘빛 x0.44 -> 날아감  0.0%  넓은 색 0.58 0.79 0.55   색 702종
```

고른 것은 **볕 든 흙이 팔레트에 적힌 제 색(0.64, 0.84, 0.58) 그대로 나오는
지점**이다. 볕이 색을 씻어내지 않는 자리다.

- 햇빛 1.10 → 0.42
- 낮 하늘빛 (0.86, 0.90, 0.95) → (0.43, 0.46, 0.52)

### 밤도 함께 낮췄다

낮만 낮추면 낮과 밤의 차이가 줄어 밤이 밤 같지 않다. 밤 하늘빛과 달빛을 같은
비율로 낮춰 차이를 지켰다.

| | 전 | 후 |
|---|---|---|
| 낮 평균 밝기 | 0.96 (74% 날아감) | 0.74 |
| 밤 평균 밝기 | 0.54 | 0.36 |
| 화면 색 종류 | 544종 | 751종 |

밤은 여전히 지면과 나무와 사람과 부품이 다 보인다.

**빛의 세기는 `SkyView` 가, 빛깔은 `Palette` 가 정한다.** 한 상수에 섞으면
색을 고르는 일과 노출을 맞추는 일이 얽힌다.

### 검사를 더했다

스크린샷 검증에 **"날아간 화면"** 을 더했다. 검은 화면의 반대쪽이다. 너무
어두워도 너무 밝아도 아무것도 보이지 않는다.

**예전 값으로 되돌리면 낮 9단계가 모두 이 검사에 걸리는 것까지 확인했다.**

테스트 614개 통과. 스크린샷 10단계 이상 0건.

---

## 형태·연출과 에셋 (feat/shapes-and-scene)

"에셋과 디자인을 추가"라는 지시를 받고, 먼저 엔진 안에서 할 수 있는 것을 하고
그 다음 바깥 에셋을 들였다.

### 화면에서 찾은 것 두 가지

**열린 문과 닫힌 문이 똑같았다.** 같은 색 같은 큐브였다. 자동문은 스펙 §5 의
첫 장치이고 §8 의 첫 판정 지점인데 **그 결과가 눈에 보이지 않았다.** 열린 문은
한쪽으로 물러난 얇은 판으로 그려 지나갈 자리가 비어 보이게 했다.

**캐릭터가 어느 쪽을 보는지 알 수 없었다.** 공 하나였다. 겨냥한 곳이 없으면
바라보는 앞 칸에 놓이는데(스펙 §3.4) 그 앞이 어딘지 화면에 없었다.

### 생김새로 가른다

색만이 종류를 가르는 단서였다. 파스텔은 쓸 수 있는 폭이 좁아 열몇 종이 되면
흐려진다. 한 칸이 화면에서 사십 픽셀 남짓이라 잔 무늬는 사라지므로
**키와 바닥 넓이로** 갈랐다.

| 부품 | 실루엣 |
|---|---|
| 눈 | 렌즈가 곧게 솟았다. 가장 키가 크다 |
| 손 | 좁은 몸통 위에 넓은 판. 버섯처럼 위가 넓다 |
| 되풀이 | 층층이 좁아지며 올라간다 |
| 상자 | 뚜껑이 몸통보다 넓게 나와 턱을 만든다 |
| 갈림길 | 둘로 갈려 올라간다. 사이가 비어 있다 |
| 묶음 | 열십자로 동여맨 꾸러미 |
| 밭 | 가장 낮게 깔린다. 밟고 다니는 곳이다 |

면마다 밝기를 구워 둔다. 빛 하나로는 윗면과 옆면의 차이가 약해 쌓아 올린 것이
덩어리로 보인다. 아이소메트릭에서 함께 보이는 옆면 두 쪽도 갈라 두었다.

### 캐릭터와 물가

몸통과 팔다리를 세우고 **모자챙이 보는 쪽을 가리킨다.** 몸도 그 쪽으로 돌아선다.

섬 끝이 허공으로 끊겨 어디가 뭍이고 어디가 바깥인지 알 수 없었다. 물낯을 지면
윗면보다 조금 낮게 깔아 물가가 보이게 했다. 표현일 뿐이고, 바다로 못 나가는
것은 이동 규칙이 이미 막고 있다.

### Kenney CC0 에셋 — 지난번 보고가 틀렸다

지난번에 "자산 페이지의 다운로드가 JS/폼으로 걸려 있어 명령줄에서 zip 을
집어낼 수 없었다"고 적었다. **틀렸다.** 내려받는 링크는 페이지 HTML 안에
그대로 적혀 있다. 브라우저 없이 받았다.

```bash
curl -sSL https://kenney.nl/assets/nature-kit   | grep -oE 'https://kenney.nl/media/[^" ]+\.zip'
```

원본 팩은 330종에 형식 여섯 가지, 10MB 다. 저장소에 다 넣을 이유가 없어
**땅에 깔 것 열두 개만 glTF 로 골라 담았다. 80KB 다.** 라이선스 파일과 받는
법을 `assets/README.md` 에 함께 남겼다.

**모양만 받고 색은 팔레트가 준다.** 가져온 모델은 제 색을 달고 오는데 그대로
두니 어둡고 톤이 흩어졌다. "색은 `view/palette.gd` 한 곳에 모은다"는 규칙은
바깥에서 온 것에도 그대로 걸린다. 파스텔 검사도 함께 훑는다.

어디에 무엇이 나는지는 **칸 좌표에서 뽑는다. 난수를 쓰지 않는다.** 표현이 RNG 를
당겨 쓰면 그 순간 결정론이 깨진다. 흙 윗면에만 나고, 그 위에 무언가를 놓으면
그 자리의 풀은 사라진다. 흙을 더 쌓으면 죽지 않고 올라선다.

### 스크린샷이 두 단계 늘었다

- `11_parts` — 부품을 나란히 놓고 카메라를 당겨 실루엣을 견준다
- `12_island` — 멀리 물러나 물가를 본다

캐릭터 판정 상자를 시점 배율에 맞춰 줄였다. 멀리서는 캐릭터가 작아지는데 상자가
그대로면 빈 땅만 세게 되어 "안 그려졌다"고 잘못 판정한다.

테스트 642개 통과. 스크린샷 12단계 이상 0건.

---

## 제작법 (feat/crafting)

스펙 §6 이 남겨 둔 "자원 3종, 제작법 10개 이내" 다. 일곱 개를 넣었다.

| 만드는 것 | 드는 것 |
|---|---|
| 문 | 나무 4 |
| 밭 | 흙 3 + 나무 1 |
| 감지기 | 광석 2 + 나무 1 |
| 작동기 | 광석 1 + 나무 2 |
| 되풀이 | 광석 3 + 나무 1 |
| 상자 | 나무 3 + 광석 1 |
| 갈림길 | 광석 2 + 나무 2 |

**부품에는 반드시 광석이 든다.** 스펙 §3.6 이 광석의 용도를 부품 제작으로
두었고, 자원지가 기지에서 떨어져 있는 이유가 그 왕복이기 때문이다. 테스트가
이 성질을 못박아서, 나중에 값을 조정하다 실수로 광석을 빼면 걸린다.

**`GIVE_STARTING_KIT` 을 껐다.** 스펙 §3.1 이 "제작이 생기면 꺼서 되돌린다"고
적어 둔 그대로다. 이제 빈손으로 시작해 나무를 부수고 광석을 캐서 만든다.
손으로 살펴볼 일이 있으면 상수 하나로 되돌릴 수 있다.

만드는 데 걸리는 시간도 제작대도 없다. 재료가 있으면 그 틱에 만들어진다.
회로가 코어이고 나머지는 최소한만 둔다는 원칙을 따랐다. 드는 재료는 화면
아래 한 줄에 뜨고(`C` 로 만든다), 모자란지는 말하지 않는다 — 손에 든 것은
핫바에 다 적혀 있다.

곁들여 고친 것 둘:

- `test_the_player_starts_with_parts_to_try` 는 헬퍼가 방금 채운 인벤토리를
  다시 세는 **자기충족 테스트**였다. 지급을 껐는데도 통과하길래 들여다보고
  알았다. 실제 지급을 보도록 고쳤다
- 파 놓은 구덩이에 물이 차 보였다. 시뮬레이션에는 빈 칸인데 화면이 물이라고
  말한 것이다. 물낯을 부술 수 없는 바닥층 아래로 내렸다

---

## 저장/불러오기 (feat/save-load)

스펙 §6 의 마지막 항목이다.

**상태를 통째로 뜨지 않는다.** 시드와 명령 기록과 틱 수만 적는다. 불러올 때는
같은 시드로 섬을 세우고 같은 명령을 같은 차례로 넣은 뒤 그만큼 다시 돌린다.

이 게임의 뿌리 규칙이 "같은 시드 + 같은 명령 → 같은 상태"이므로(스펙 §2)
**그 규칙 자체를 저장 형식으로 삼았다.** 스냅숏은 필드 하나만 빠뜨려도 조용히
어긋나지만, 이쪽은 규칙이 깨지면 결정론 회귀 테스트가 먼저 운다.

### 값을 먼저 재고 정했다

```
  1200틱 (  60초 분량) ->   13ms
 12000틱 ( 600초 분량) ->   93ms
 36000틱 (1800초 분량) ->  281ms
```

30분 분량이 281ms 면 스냅숏을 만들 이유가 없다. 판이 아주 길어져 거슬리면
그때 얹는다. 그때도 이 기록은 남겨 두는 편이 낫다 — 어긋났을 때 견줄 것이
있어야 한다.

### 알려진 성질 하나

**명령을 거치지 않은 상태 변경은 되살아나지 않는다.** 게임 자체는 모든 것을
명령으로 하므로 문제가 없다. 다만 스크린샷 러너는 무대를 세우려고 상태를
손으로 건드리는데, 그래서 저장 검사 단계를 **손대기 전인 앞쪽**에 두었다.

이것은 한계가 아니라 CLAUDE.md 의 "모든 상태 변경은 명령 객체를 경유한다"를
드러내는 자리다. 테스트가 그것도 못박는다.

F5 로 적고 F9 로 되살린다. 화면에 아무 변화가 없어 알 수 없으므로 한 줄
알림을 붙였다.

테스트 684개 통과. 스크린샷 14단계 이상 0건.

---

## 스팀 QA 피드백 1차 반영 (fix/qa-round-1)

스팀 인디게임 QA 에이전트를 만들어(`.claude/agents/steam-indie-qa.md`) 검수를
받고 고쳤다. 열두 항목이 왔고 열하나를 처리했다.

에이전트 지침에 두 가지를 못박아 두었다. **근거 없는 항목을 쓰지 못한다**
(무엇을 보고 그렇게 판단했는지 대야 한다), 그리고 **스펙이 일부러 정한
제약을 고치라고 하지 못한다**(에러 메시지 없음, 게이팅 없음, 프로그래밍 용어
노출 금지는 설계다 — 그 안에서 답을 찾게 한다).

### 치명 넷

**1. 배선이 화면에 없었다.** 스크린샷 14장 어디에도 배선이 한 픽셀도 없었다.
칸 한가운데끼리 곧게 이었는데 부품이 자기 칸을 거의 채우는 메시라서, 맞닿은
두 부품을 이으면 배선이 통째로 부품 안에 파묻혔다. 굵기 0.08 은 한 칸이
사십 픽셀인 화면에서 세 픽셀이다.

**회로 게임인데 회로가 화면에 없었다.** 스펙 §4.1 의 "흐르지 않는 배선은
흐린 색으로 보인다"는 "원인을 알려주지 않음"의 **유일한 보상**인데 그 약속이
지켜지지 않고 있었다. 부품 지붕 위로 넘어가는 세 토막으로 바꿨다.

**2. 첫날 밤에 닿을 수가 없었다.** 배는 4분에 비고 5분 40초에 쓰러지는데
첫 밤은 7분에 온다. 그런데 섬에 손으로 먹을 것이 하나도 없었다 — 밭에서
거두는 것은 작동기뿐이고 작동기를 만들려면 스무 칸 밖 자원지를 다녀와야 한다.
난이도가 아니라 막다른 길이었다. 시작 자리 둘레에 저절로 난 작물 열넷을 두었다.

**3. 소리가 한 줄도 없었다.** 프로젝트 전체에 오디오 0건. 스펙 §8 이 판정
지점으로 잡은 조작감의 절반이 소리다. Kenney CC0 에서 열셋을 골라 넣었다.

**4. 첫 화면에 무엇을 해야 하는지 없었다.** 전부 0 인 핫바 열한 칸과 얼굴
없는 흰 공뿐. 조작 안내는 숨겨져 있고 그것을 여는 키가 H 라는 것도 알려 주지
않았다. 낮밤 띠와 첫 동사 세 줄을 두었다.

### 그 밖

| 지적 | 고친 것 |
|---|---|
| 나무가 갈색 막대로 보임 | 줄기 위에 잎을 덧그림. 담 위에는 안 남 |
| 놓은 흙이 땅에 묻힘 | 윗면은 풀, 옆면은 흙으로 가름 |
| 밤이 흐린 낮 | 밝기가 아니라 **빛깔**을 파랑으로 |
| 위협이 화면 밖에서 나옴 | 8~14칸 띠로. 다가오는 것이 보인다 |
| 캘 수 있는 돌과 못 캐는 잔돌이 같은 색 | 잔돌을 아예 뺌 |
| 설정이 다른 부품이 다 똑같이 생김 | 겨냥하면 읽어 줌 + **상자 세 모양**(스펙 미구현이었다) |
| 묶음 조작이 숨겨진 목록 열째 줄 | 부품을 겨냥하면 그 줄이 V 를 알려 줌 |
| 광석 자원지가 안 보임 | 가운데가 솟은 더미로 |
| 위협이 사람과 구별 안 됨 | 납작하고 넓은 덩어리로, 통통 튀게 |

### 스토어용 그림을 러너에 넣었다

QA 가 "14장 중 스토어 첫 스크린샷으로 쓸 그림이 없다"고 했다. 그래서
`15_store` 단계를 더했다. **새 판에서** 해 질 녘에 감지기→갈림길→되풀이→
상자→작동기→문 한 줄을 눕히고, 신호가 흐르는 순간을 잡고, 그 다섯을 삼킨
묶음 한 칸을 바로 옆에 놓는다.

러너가 결정론적으로 같은 그림을 다시 뽑아 주므로 아트가 바뀔 때마다 이
그림도 함께 갱신된다. **검사 도구가 곧 마케팅 자산이다.**

### 고치지 않은 것

QA 가 "첫 주기만 포만도 감소를 늦추자"고 제안한 것은 하지 않았다. 스펙 §3.7
이 정한 숫자이고, 먹을 것을 둔 것으로 막다른 길은 풀린다. **안 먹으면
여전히 낮이 끝나기 전에 다친다**는 것을 테스트가 못박는다. 그것까지 없애면
생존이 사라진다.

### 완료 조건

테스트 747개 통과(684 → 747). 스크린샷 15단계 이상 0건.
골든 해시는 섬이 세 번 바뀌어(작물·자원지·위협 자리) 그때마다 갱신했다.

---

## 세계를 깊게 (feat/deep-world)

"전체적인 시스템이 마인크래프트처럼" 이라는 지시. 먼저 어디까지를 뜻하는지
확인했다 — **스펙 안에서 세계를 깊게, 회로는 코어 그대로.** 시점과 무한 지형은
스펙이 일부러 배제한 것이라(§3.1, §3.5) 건드리지 않았다.

### 재고 시작해서 찾은 것

지하를 파기 전에 비용부터 쟀는데 거기서 걸렸다. **블록을 하나 놓을 때마다
격자 육만 오천 칸을 통째로 다시 훑고 있었다. 66ms 다.** 한 프레임이 16.7ms
이니 놓을 때마다 화면이 네 프레임씩 멎었다. 지하를 채우면 275ms 가 된다.

QA 가 "손맛 — 누른 것이 즉시 반응하는가"를 판정 항목으로 꼽았는데 그 값이
여기 있었다. 블록 하나가 바뀌면 겉모습이 달라지는 것은 그 칸과 이웃 여섯
칸뿐이므로, 격자가 어디가 달라졌는지 적어 두고 그 일곱씩만 다시 본다.

```
통째로 다시 그리기 -> 66.010ms
놓기 한 번         ->  0.018ms
```

빨라지는 대신 틀릴 수 있게 되었으므로, 테스트가 지키는 것은 하나다 —
**고쳐 그린 결과가 통째로 다시 그린 결과와 같은가.**

### 세로를 쓴다

64x64x16 중 실제로 쓰던 것은 두 층이었다. 바닥층은 부술 수 없으니 **팔 수
있는 땅이 한 층뿐**이었고, 그러면 캐는 일이 성립하지 않는다.

- 세로를 16 → 24 로 늘리고 지표를 8 로 올렸다
- **지표가 기복을 탄다.** 성긴 격자에 값을 두고 사이를 곧게 잇는다. 칸마다
  따로 뽑으면 소금을 뿌린 것처럼 되어 지형이 이어지지 않는다
- **이웃한 기둥끼리 한 칸 넘게 벌어지지 않는다.** 두 칸 턱은 오르지 못하므로
  그것이 흔해지면 섬이 걸어 다닐 수 없는 곳이 된다. 시작 자리에서 걸어
  섬의 99% 에 닿는 것을 테스트가 지킨다
- 지표 아래는 **돌**이다. 광석과 돌이 같은 것이면 지하를 채우는 순간 광석이
  흔해져 자원지가 뜻을 잃는다
- **광맥**은 깊을수록 흔하다. **동굴**이 뚫려 있다
- 물가를 고르게 눕히고 바다를 **고리**로 둘렀다. 평평한 판이면 땅을 파
  내려가는 순간 구덩이에 물이 차 보인다 — 없는 것을 있다고 말하는 셈이다

### 땅속이 땅속이 되려면

파고 내려갔는데 갈 이유가 없었다. 세 가지가 빠져 있었다.

1. **대낮처럼 밝았다.** 지표에서 멀어질수록 어둡게 그린다
2. **등이 없었다.** 스펙 §4.2 의 작동기 표와 §5 의 자동 조명에 등이 적혀
   있는데 블록이 없었다 — **회로가 세상을 바꾸는 자리가 문 하나뿐이었다.**
   넣고 나니 스펙 §5 의 다섯 장치 중 자동 조명이 실제로 돈다
3. **지붕 아래가 보이지 않았다.** 고정 아이소메트릭의 성질이다. 머리 위가
   막혀 있을 때만 그 층 위를 걷어낸다. 트여 있으면 그대로 둔다 — 늘
   걷어내면 제가 세운 건물이 사라진다

처음에는 머리 위 둘레만 도려냈는데 방의 벽이 함께 사라져 섬에 구덩이가
뚫린 것처럼 보였다. 층 전체를 걷어내는 쪽이 맞다.

### 미뤘던 것 — 짊어질 수 있는 양

여기서는 접었다. 넘치는 것을 둘 곳이 없었기 때문이다. **다음 작업에서
궤짝과 함께 넣었다.**

### 완료 조건

테스트 779개 통과(747 → 779). 스크린샷 16단계 이상 0건.
골든 해시는 섬이 세 번 바뀌어(지형·해안·등) 그때마다 갱신했다.

---

## 인벤토리와 궤짝 (feat/real-inventory)

"시스템이 마인크래프트가 아니다. 인벤토리같은 것도 없다"는 지적. 맞다.
예전 것은 인벤토리가 아니라 **종류마다의 개수 하나**였다. 한계가 없고,
칸이 없고, 옮길 수도 없었다.

그러면 무엇을 가져갈지 고르는 일이 아예 없다. 자원지를 오가는 일에 값이
붙지 않으니 스펙 §3.6 의 "왕복이 자동 운반 장치의 동기가 된다"가 성립하지
않는다. **손이 먼저 모자라야 그 다음이 있다.**

### 칸

- **36칸, 칸마다 64개까지.** 앞 9칸이 손에 잡히는 줄이다
- 칸 하나는 [무엇, 몇 개, 어느 것] 이다. **어느 것**은 묶음처럼 종류만으로는
  가려지지 않는 물건의 자리다. 둘 다 같아야 한 칸에 쌓인다
- **핫바가 종류가 아니라 칸이 되었다.** 1~9 로 칸을 잡고 잡은 칸에 든 것을
  놓는다. 빈 칸도 잡을 수 있다. 묶음도 칸에 들어가므로 따로 쥐는 키가
  필요 없어졌다
- **손이 차면 부수지 못한다.** 부수고 나서 버리면 조용히 사라지고, 땅에
  떨어뜨리자니 떨어진 것을 주울 길이 아직 없다. 만들기와 묶기도 받을 자리가
  있어야 한다 — 재료만 쓰고 결과가 사라지면 안 된다

### 화면과 궤짝

**Tab 또는 I** 로 서른여섯 칸을 펼친다. 마우스로 집어 옮기고, 옆에 늘어선
만들 것 목록을 눌러 만든다.

**궤짝**(나무 6, 27칸)을 겨냥하고 E 를 누르면 열린다. 손과 궤짝 사이로
물건을 옮긴다. 안이 빈 궤짝만 걷어낼 수 있다 — 부수면 안의 것이 조용히
사라진다.

**옮기는 것도 명령을 거친다.** 화면이 인벤토리를 직접 고치면 저장한 판을
되살렸을 때 물건이 제자리로 돌아간다. 테스트가 그것을 못박는다.

### 그리기 버그를 하나 잡았다

**MultiMesh 의 instance_count 를 한 칸씩 늘리면 그때마다 버퍼를 다시 잡아
앞서 넣어 둔 것이 지워진다.** 블록을 놓을 때마다 화면이 무너지고 있었다.
넉넉히 잡아 두고 보이는 수(visible_instance_count)만 조절한다.

바로 전 커밋에서 "바뀐 칸만 고친다"로 바꾸면서 들어간 것이고, 스크린샷
검증이 잡았다.

### 팔레트 검사를 실제 요구에 맞췄다

파스텔은 쓸 수 있는 폭이 좁다(명도 0.60 이상, 채도 0.45 이하). 블록이 열몇
종을 넘어가면 색만으로는 더 가를 자리가 없다 — 궤짝을 넣을 때 실제로 막혔다.

이 게임은 처음부터 **키와 바닥 넓이로도 가르기로** 했으므로 검사도
"색이든 모양이든 하나로는 갈려야 한다"로 고쳤다. 다만 색이 겹치는 짝이
늘어나면 화면이 한 덩어리로 보이므로 그 수에 한계를 두었다.

### 완료 조건

테스트 819개 통과(779 → 819). 스크린샷 17단계 이상 0건.

---

## 스팀 QA 피드백 2차 반영 (fix/qa-second-pass)

2차 QA 판정도 **"안 붙는다"** 였다. 1차 때와 이유가 달랐다 — 화면에는 이제
회로가 보이는데, **손이 죽어 있고 화면이 읽히지 않는다**는 것이었다.

### 손이 죽어 있었다

마우스로 아무것도 할 수 없었다. 마인크래프트를 기대하고 켠 사람이 처음
삼십 초에 하는 일이 좌클릭인데, 그것이 아무 일도 하지 않았다.

- 좌클릭 부수기 · 우클릭 놓기(Q/E 도 그대로 둔다), ESC 로 닫기
- 휠로 손에 잡힐 칸을 고른다. `+`/`−` 로 멀어지고 가까워진다
- 누르고 있으면 이어서 파진다(`DIG_TICKS`)
- 인벤토리에서 좌클릭으로 집고 놓고, **우클릭으로 절반을 가른다**
  (`Inventory.split`, `MoveItemCommand.split`). 광석 마흔여덟 개를 한 칸씩
  옮기는 것이 아니라 절반씩 가르는 것이 그 손놀림이다

### 화면이 읽히지 않았다

- **칸에 그림이 들어갔다**(`view/block_icon.gd`). 아홉 칸에 이름이 적혀
  있으면 훑을 수 없고 매번 읽어야 한다. 세상에 세우는 것과 **같은 상자
  표**(`BlockMeshes.boxes_of`)를 읽어 그리므로 인벤토리의 실루엣과 땅 위의
  실루엣이 같다. 표를 두 벌 두면 언젠가 어긋나고, 어긋난 그림은 없느니만 못하다
- **놓인 부품의 설정이 지붕 위에 얹힌다**(`view/part_marks.gd`). 부품은 다섯
  종뿐이고 나머지는 전부 설정으로 갈리는데, 그 설정이 안 보이면 감지기 스무
  개가 놓인 판은 하나씩 겨냥해야만 읽을 수 있었다. 감지기 대상 다섯, 갈림길
  판정, 되풀이 셋, 상자 모양 셋, 묶음 크기 셋이 형태로 갈린다. 글자도 기호도
  쓰지 않는다 — 서 있는 것 · 하늘을 보는 접시 · 싹처럼 뜻이 형태에 담긴 것만
- **배선에 방향과 흐름이 생겼다.** 도착 쪽으로 갈수록 가늘어지고, 흐르는
  배선 위로 밝은 점이 출발에서 도착으로 되풀이해 달린다. 켜지는 순간에 한 번만
  태워 보내면 이미 돌고 있는 회로를 나중에 본 사람에게는 아무 일도 없는
  화면이 된다
- 만들 수 있는 것 목록에 그림과 드는 재료가 함께 선다. 마우스를 얹은 칸의
  이름은 이름표가 따라온다

### 소리가 화면과 다른 것을 말했다

손에 든 총량이 늘고 줄었는지로 짐작하던 것을, 격자에게 직접 묻도록 고쳤다
(`VoxelGrid.changes_since`). 밭을 거두어도 곡괭이 소리가 나고 무언가를 만들어도
널빤지 소리가 겹치던 것이 사라졌다. 소리는 나는 자리에서 난다.

### 막혔는데 아무 일도 안 일어났다

손이 차면 부수기가 통째로 막히는데, 화면에서는 키가 안 먹는 것과 구별되지
않았다. 둔한 소리 하나와 줄의 짧은 흔들림으로 알린다. 무엇이 막았는지는
말하지 않는다.

**규칙을 표현 레이어에 옮겨 적지 않았다.** 손이 찼는지 궤짝이 비었는지를
화면이 다시 판단하면 언젠가 시뮬레이션과 어긋난다. 명령을 낸 다음 **세상이
그대로인지만** 본다.

### 땅속에 갈 까닭이 없었다

광맥이 바위 속에만 들어서, 굴을 파고 들어가도 회색 벽뿐이라 어디를 파야 할지
몰랐다. 드러난 벽에 더 잘 들게 했다. 벽에만 나지는 않으므로 캐고 나서 더 파고
들어갈 까닭도 남는다.

### 상점 그림에 장치가 한 일이 없었다

해 질 녘이라 무엇을 하는 게임인지가 어둠에 묻혔고, **회로가 문을 열지도
못하고 있었다.** 화면의 오른쪽은 격자로 (1,-1) 이라 대각선인데 작동기는 맞닿은
여섯 쪽만 건드린다 — 나란히 보이던 문이 실은 손이 닿지 않는 자리에 있었다.
한낮으로 옮기고, 문을 맞닿은 자리로 옮기고, 켜지는 등을 하나 붙였다. 검사
도구가 이제 그것을 확인한다(`_store_door_open`).

곁들여 **한낮의 등이 바닥을 하얗게 날리던 것**도 고쳤다. 켜진 등이 아니라
잘못 그려진 흰 상자로 보였다. 세기가 그 자리의 어둠을 따른다 — 밤과 땅속에서는
온 힘을 내고 한낮 지표에서는 있는 듯 마는 듯하다.

### 검사 도구가 가끔 거짓으로 울었다

캐릭터를 감춘 뒤 세 프레임만 기다려서, 처음 도는 판에서 프레임이 밀리면 두
장이 같게 잡혔다. **검사 도구가 거짓으로 우는 것이 가장 나쁘다** — 열 프레임으로
늘렸다.

### 테스트를 고친 것

세 곳에서 테스트가 옛 의도를 적고 있었다. 통과시키려고 고친 것이 아니라,
의도 자체가 바뀌었으므로 새 의도를 적었다.

- `test_parts_with_nothing_to_show_get_no_mark` — 감지기·갈림길에 표시가 없는
  것이 옳다고 적혀 있었다. 그것이 바로 QA 가 지적한 결함이다
- `test_only_the_endless_repeater_looks_different` — 셋이 갈리도록 바꿨다
- 핫바 칸이 이름을 적는다고 보던 것들 — 이름은 겨냥 줄이 맡는다

### 골든 해시

지형이 바뀌어 넷이 깨졌다. 서로 다른 프로세스에서 두 번 같은 값이 나오는 것을
확인하고 갱신했으며, 무엇이 바뀌었는지 이력 줄을 남겼다.

### 완료 조건

테스트 871개 통과(867 → 871). 스크린샷 17단계 이상 0건.

---

## 스팀 QA 피드백 3차 반영 (fix/qa-third-pass)

3차 판정도 **"안 붙는다"** 였는데, 이번 이유는 앞의 둘과 달랐다.

> **만든 것이 부족한 게 아니라 만든 것이 화면 밖에 있다.**

주장한 여덟 가지 중 여섯은 코드에 실물로 있었다. 그 중 넷이 화면에 도달하지
못하고 있었던 것이다. QA 가 화소를 세어 왔다 — 흐르는 배선 넷 위를 점 넷이
달리는 그림에서 흰빛 화소가 174개, 그나마 대부분 등이었다.

### 자동문 그림 석 장이 죽은 회로를 찍고 있었다

감지기를 넉 칸 떨어뜨려 놓았는데 `DetectorPart.SENSE_RADIUS` 가 3 이다.
감지기가 사람을 볼 수 없으니 배선은 내내 흐린 회색이었고, 그 뒤 두 장은 **죽은
회로를 고르고 죽은 회로를 묶는** 그림이었다. 스펙 §8 이 "1번이 재미없으면
부품을 늘려도 소용없다"고 지목한 바로 그 판정 지점이다.

문 열림 검사를 상점 그림 전용으로 달아 두었던 것이 화근이었다. **회로가 나오는
모든 단계**의 검사로 넓혔다. 그 검사가 처음부터 그랬으면 이건 안 나왔다.

### 만들어 놓고 안 보이게 둔 것들

- **신호 점을 배선과 같은 색으로 칠했다.** 희게, 굵게 바꿨다
- **가늘어지는 것이 정작 보이는 구간에는 없었다.** 굵기를 꺾이는 마디 단위로
  정해서, 눈에 보이는 가운데 토막은 굵기가 한결같았다. 맞닿은 두 부품 사이 —
  회로에서 가장 흔한 배치 — 에서는 방향이 전혀 안 보였다. 열두 토막으로 쪼갰다
- **배선이 기본 배율에서 여섯 픽셀이었다.** 바짝 당겨 찍은 상점 그림에서만
  보였다. 회로가 코어인 게임에서, 사람이 보통 서 있는 거리에 회로가 없었다
- **표시 열여덟 종이 한 색이었다.** 열두 픽셀짜리 같은 색 알갱이라 스무 개
  놓이면 그냥 알갱이 스무 개다. 갈래마다 색을 주고 크기를 키웠다 — 형태로
  갈래 안을 가르고, 색으로 갈래끼리 가른다
- **부품 진열 그림에 그 표시가 하나도 없었다.** 격자에만 놓고 회로에는 안
  넣어서다([PartMarks] 는 회로를 읽는다). 이번에 만든 기능을 보여야 할 그림에
  그 기능이 없었다
- **광석이 어둠에 먹혔다.** 벽 편향은 코드에 있었지만 캄캄해서 검은 벽과
  구별되지 않았고, 지하 그림은 아홉 칸 사방을 통째로 파서 그 안의 광맥까지
  지우고 찍었다. 광석이 어둠을 덜 먹게 하고, 좁은 굴을 뚫어 벽이 생성기가
  만든 그대로 남게 했다

### 마우스를 살려 놓고 첫 화면은 키를 가리켰다

첫 줄이 `"부수기 Q · 놓기 E · 조작 안내 H"` 였다. **안 알려준 기능은 없는
기능이다.** 걷는 법을 알려주는 줄도 없었고, 부수는 법이 그보다 먼저 나왔다.

### 만들기만 규칙을 옮겨 적고 있었다

재료를 미리 세어 보고 성공 소리를 곧바로 냈다. 시뮬레이션이 다른 까닭으로
거절하면 소리는 "만들었다"고 말한다 — 화면과 소리가 다른 것을 말하는 자리다.
부수기·놓기와 같은 방식(낸 다음에 손이 바뀌었는지만 본다)으로 맞췄다.

### 등 목록이 세계를 통째로 훑었다

9만 8천 칸을 삼중 루프로 도는 일이 **블록을 하나 부술 때마다** 벌어졌다.
파고 내려가는 것이 새로 붙인 유인인데 그 동작이 프레임을 가장 자주 흔들었다.
변경 기록(`changes_since`)만 따라가고, 기록이 넘칠 만큼 밀렸을 때만 훑는다.

### 열일곱 장 중 두 장이 같은 그림이었다

만들기 그림은 만들었다는 사실이 손에 잡히는 줄의 숫자로만 나타났다. 만든 문을
실제로 세우고 하나는 손에 남긴다 — 손이 비면 밑동 한 줄이 "빈 손"이라고 적어
방금 만든 것이 무엇인지 그림이 말하지 못한다.

### 상점 그림에서 묶음이 여섯째 부품으로 뭉개져 있었다

줄 끝에 나란히 세워 두었더니 다른 부품과 구별되는 것이 아무것도 없었다.
**이 게임이 복셀 게임 백 개와 갈리는 단 하나의 문장**(스펙 §4.3)이 그림에서
사라진 것이다. 줄 위에 홀로 세워 "저 다섯이 이 한 칸"으로 읽히게 했다.

### 곁들여

안내판의 깨진 글자 둘(궤짝 · 잇기)을 고쳤다. **안내판은 조작을 배울 수 있는
유일한 곳**이고, 거기서 한 줄이 깨져 있으면 나머지 스무 줄의 신뢰도가 같이
내려간다. 단계 이름이 바뀌며 남은 낡은 그림 두 장도 치웠다.

### 테스트를 고친 것

`test_the_very_first_line_names_the_first_keys` 가 첫 줄에 "Q" 가 있어야 한다고
적고 있었다. 그것이 바로 QA 가 지적한 결함이라, 새 의도(걷는 법과 마우스가
먼저)를 적었다.

### 완료 조건

테스트 886개 통과(871 → 886). 스크린샷 17단계 이상 0건.

---

## 조작·움직임 QA 반영 (fix/stepping-down)

지적은 한 줄이었다.

> **높은 지형에서 낮은 지형으로 이동할 때, 이동키를 유지하면 높이가 유지됨.**

재어 보니 그냥 그런 것이 아니었다. 시드 1234 의 섬에서 시작 자리를 잡고 한
방향으로 키를 눌러 보면, 사람이 단을 벗어난 뒤 **그 높이 그대로 x=5, 6, 7 로
계속 걸어갔다.** 벼랑 위 허공을 걸어 섬 반대편까지 간다.

### 왜 아무도 못 잡았나

낙하 코드는 있었다. `Simulation._settle_character` 가 매 틱 발밑을 보고 비었으면
한 칸 내린다. 규칙도 맞게 적혀 있었다 — *"걸음은 걸음이고 낙하는 낙하다."*

문제는 **두 숫자가 정확히 맞물려 있었다**는 것이다.

| | 값 |
|---|---|
| 한 걸음에 걸리는 틱 | `SUBUNITS / WALK_SPEED` = 1000 / 250 = **4** |
| 이동 키를 누르고 있을 때 걸음 간격 | `InputController.REPEAT_TICKS` = **4** |

한 걸음이 끝나는 바로 그 틱에 다음 걸음이 들어온다. `step()` 은 명령을 먼저
적용하고 낙하를 나중에 본다. 그래서 **키를 누르고 있는 동안 낙하는 한 번도
차례를 얻지 못했다.** 손을 떼면 그때 떨어진다 — 그래서 눌러 보지 않고
화면만 보면 멀쩡하다.

유닛 테스트도 이걸 못 잡았다. `resolve_walk` 를 한 번씩만 부르고 있었기
때문이다. 한 걸음은 다 옳았다. **키를 누르고 있는 것**이 틀렸다.

### 고친 것

**떨어지는 중에는 걸음이 시작되지 않는다.** `MovementRules.is_falling` 을
새로 두고 `resolve_walk` 와 `_settle_character` 가 같은 판정을 쓰게 했다.
격자 바닥과 아래가 막힌 자리는 떨어지지 않는 것으로 본다 — 거기서까지
걸음을 막으면 갇힌다.

**한 칸 턱은 오르는 것처럼 내려선다.** 이건 지적에 없던 것인데, 위만 고치면
비탈을 내려가는 것이 "걷다 - 뚝 - 걷다 - 뚝" 이 된다. 오르는 걸음이 4틱인데
내려가는 걸음만 4+2 틱이 되기 때문이다. 세계가 깊어지며 기복이 생긴 뒤로
이 차이가 걸음마다 나온다. 두 칸 이상은 그대로 벼랑이다 — 걸어 나간 뒤
떨어진다.

**내려가는 걸음은 걸음 속도로 간다.** `_speed_for` 가 `remaining.z < 0` 만
보고 낙하 속도(500)를 내주고 있었다. 가로로 함께 나아가는 것은 낙하가 아니라
걸음이다. 곧게 떨어질 때(x·y 가 둘 다 0)만 빠르게 바꿨다. 안 고쳤으면
내리막만 두 배 빨라 미끄럼틀이 된다.

### 같은 뿌리에서 나온 것 둘

**위협이 허공에 떠 있었다.** `Threat` 은 `resolve_walk` 를 쓰면서 낙하 코드는
없었다. 단에서 내려온 위협은 그 높이에 뜬 채로 사람을 따라다니고, 밑을 파내도
떠 있었다. 사람과 같은 중력을 줬다.

**떨어지는 것이 뛰어가는 소리로 들렸다.** 발소리가 "칸이 바뀌면" 울고 있었다.
낙하는 2틱에 한 칸이라 세 칸 벼랑이 100ms 간격 발소리 세 방이 된다. 이건
고치기 전에는 잘 안 들렸다 — 애초에 떨어지질 않았으니까. 가로로 옮겼을
때만 울리고, 닿는 순간에 한 번 내도록 바꿨다.

### 실제 섬에서 재어 확인

시드 1234, 네 방향으로 각 400틱씩 키를 눌러 봤다. 사람이 지나온 칸의 높이가
`11 → 10 → 9 → 8 → 7 → 8 → 9 → 10 → …` 으로 땅을 따라간다. 멈춘 채 떠 있던
틱은 방향당 0~4 였고, 그것도 전부 **두 칸 벼랑에서 걸어 나간 뒤 떨어지는
정상 동작**이었다 (예: (26,32,12) 에서 나가 12 → 11 → 10). 갇히는 자리는 없다.

### 완료 조건

테스트 900개 통과(898 → 900). 스크린샷 17단계 이상 0건.
**골든 해시는 그대로다** — 회귀 시나리오에서는 내려서거나 뜨는 일이 없었다.
그게 이 결함이 여기까지 살아남은 이유이기도 하다.

### 고쳐 적은 테스트 하나

`test_stepping_off_a_ledge_walks_out_over_the_drop` 이 한 칸 아래도 같은
높이로 걸어 나간다고 적고 있었다. 그것이 바로 고친 대상이라, 두 칸 벼랑으로
옮겨 새 의도를 적었다(`test_stepping_off_a_deep_ledge_walks_out_over_the_drop`).
한 칸을 내려서는 규칙은 `test/unit/test_stepping_down.gd` 에 따로 있다.

### 밤에 눈으로 볼 것 하나

위협이 이제 벼랑을 뛰어내려 쫓아온다. 전에는 떠서 맴돌았다. **밤이 더 조여
왔을 수 있다.** 판단 17번(생존 수치)을 볼 때 함께 봐 달라.

---

## 스펙에 없어 스스로 내린 설계 판단

되돌리기 어려운 것은 없다. 대부분 상수 하나나 함수 하나로 바뀐다.

**신호 의미론 (가장 중요)**

1. **실수 신호를 1000배 고정소수점으로 담는다.** 스펙은 값 타입에 실수를 두는데
   CLAUDE.md 는 시뮬레이션의 부동소수점을 금지한다. 고정소수점이 두 요구를 모두
   지키는 유일한 길이라 봤다. 결과: 실수 정밀도가 1/1000 로 고정된다.
2. **"실행된다 = 신호가 있다".** 값이 참인지가 아니라 신호가 닿았는지로 작동한다.
   스펙 §4.2 의 감지기 설명("조건을 만족하면 신호를 출력")에서 끌어냈다.
   **대가: "없음"을 조건으로 삼을 수 없다.** "사람이 없을 때"를 만들려면 그런
   대상을 가진 감지기가 따로 있어야 한다.
3. **아직 없는 감지 대상은 거짓이 아니라 신호 없음.** 지금은 다섯 대상이 다 있어
   해당 없음.
4. **갈림길 출구는 값을 그대로 흘려보낸다.** 실행 표시만 보내면 사슬에서 값이 끊긴다.
5. **상자는 담긴 값을 늘 내보낸다.** 꺼내는 신호를 따로 두면 배선이 두 배가 된다.
6. **배선이 여럿이면 먼저 이어진 쪽이 이긴다.** 순서를 정해야 결정론이 선다.

**게임 규칙**

7. ~~**빈손으로 시작한다.**~~ 제작이 없어 회로를 시험할 길이 없었으므로
   판정용으로 지급하도록 바꿨다. `IslandBuilder.GIVE_STARTING_KIT` 로 되돌린다.
8. **지반층(z=0)은 부술 수 없다.** 뚫리면 딛을 곳 없는 칸에 갇힌다.
9. **붙여서만 놓기 / 딛을 곳 없는 칸으로 못 나감.** 허공에 뜬 블록과 바다 위
   걷기를 막는다.
10a. **대각선은 양옆이 모두 열려 있어야 지나간다.** 스펙에 없던 규칙이다.
    한쪽만 보면 벽 모서리를 뚫고 지나간다. 오를 때도 마찬가지다.
10b. **대각선과 곧은 걸음은 같은 시간이 걸린다.** 월드 거리가 아니라 화면
    길이를 맞춘 것이다. 근거는 위 "속도" 항목.
10. **걷는 중에는 다음 걸음 명령을 무시한다.** 칸 경계에 정확히 서야 다음 판정이
    맞다. 연달아 누르면 살짝 끊기는 느낌이 날 수 있다.
11. **되풀이 과열 40회, 쉬는 동안 1틱에 1씩 식음.** 스펙은 "무한 반복은 과열"만
    말한다. 정해진 횟수만 도는 되풀이까지 타면 안 되므로 식는 규칙을 넣었다.
12. **타 버린 되풀이는 부숴도 재료가 안 돌아온다.** 스펙의 "소모된 자원은
    돌아오지 않는다"를 이렇게 새겼다. 다른 해석도 가능하다.
13. **작동기는 맞닿은 여섯 칸을 모두 작동시킨다.** 한 칸만 고르게 하려면 부품에
    방향을 줘야 하는데 프로토타입에서는 너그러운 쪽이 낫다고 봤다.
14. **쓰러지면 인벤토리 절반을 잃는다.** 스펙은 "일부"라고만 한다.
15. **위협은 길이 막히면 앞을 부순다.** 회로도 부술 수 있어 밤이 실제 시험이 된다.
16. **밭은 작동기로만 거둔다.** 손으로 거둘 수 있으면 자동 농장을 만들 이유가 없다.
17. **생존 수치 전부** (`Vitals`, `Threat`, `ThreatField`, `CropField` 의 상수).
    첫날 밤을 손으로 버틸 만하게 잡은 **추측**이다.

**표현**

18. **부품 설정은 미리 정한 몇 가지 중에서 고른다.** 숫자를 자유롭게 넣을 화면이
    없다. 부품 자체는 임의의 값을 받는다.
19. **파스텔 경계 명도 0.60 / 채도 0.45, 색 구별 기준 RGB 거리 0.12.**
20. **스크린샷은 창을 띄워 찍는다.** `--headless` 는 렌더링 드라이버가 더미라
    캡처가 안 된다. 확인한 사실이며 우회로가 없다. CI 에서 돌리려면 가상
    디스플레이가 필요하다.

---

## 건너뛴 것

없다. **스펙 §6 의 프로토타입 범위가 다 찼고**, 스팀 QA 열두 항목 가운데
열하나를 고쳤다.

남은 것은 만드는 일이 아니라 **판정**이다. 스펙 §6 의 완료 판정 기준은
하나뿐이다 — *"첫날 밤을 손으로 버티고, 둘째 날 회로를 만들고 싶어지는가."*
이건 사람이 직접 해 봐야 한다.

---

## 아침에 확인할 것 (우선순위 순)

1. **직접 플레이해서 조작감을 볼 것.** `godot --path .` 후 `H` 로 조작 안내.
   스펙 §8 은 여기를 판정 지점으로 잡는다 — *"1번이 재미없으면 부품을 늘려도
   소용없다."*

2. **여덟 쪽이 손에 맞는지.** W/A/S/D 로 화면 수직수평, 두 키를 겹쳐 눌러
   화면 대각선. 조합키를 눌러야 격자 축으로 간다는 것이 자연스러운지.
   모서리에서 막히는 느낌이 답답하지 않은지도 봐 달라.

2b. **오르내리는 손맛.** 키를 누른 채 비탈을 오르내려 볼 것. 한 칸 턱은
   오르내리는 데 같은 시간이 걸리게 맞췄고, 두 칸 이상은 걸어 나간 뒤
   떨어진다. 벼랑에서 잠깐 허공에 나가 있는 것이 어색한지, 아니면
   내려서는 것과 떨어지는 것이 갈려 있는 편이 나은지 봐 달라.

3. ~~갈림길 출구 고르기~~ — 붙였다. T 로 바꾸고 색으로 구별된다. 위 "건너뛴 것" 참조.
   이것 없이는 갈림길의 절반이 손에 닿지 않는다.

4. **부품 다섯 개짜리 장치를 손으로 지어 볼 것.**
   눈 → 갈림길 → 되풀이 → 상자 → 손 → 문. 만들면서 답답한 지점을 봐 달라.

4b. **그것을 묶어 볼 것.** V 로 고르고 B 로 드나들 자리를 정하고 G 로 묶는다.
   N 으로 쥐고 E 로 놓는다. 스펙 §8 의 마지막 검증 단계다 — *"묶음 → 만든 걸
   압축해 재배치"*. 고르는 손놀림이 번거롭지 않은지, 드나드는 자리가 고른
   차례로 정해지는 것이 헷갈리지 않는지 봐 달라.

5. **밤을 한 번 겪어 볼 것.** 위협이 회로를 부수는 것이 재밌는 긴장인지 그냥
   짜증인지가 판단 15번의 갈림길이다.

6. **생존 밸런스.** 판단 17번은 전부 추측이다. 포만도 4분, 작물 90초, 위협 4마리,
   위협 속도 0.6초/칸. 첫날 밤이 버틸 만한지가 기준이다.

7. ~~지급을 언제 끌지~~ — 껐다. 이제 빈손으로 시작한다. **첫날이 너무
   빡빡하지 않은지 봐 달라.** 나무 4 로 문 하나, 광석까지 다녀와야 부품이
   하나 나온다. 스펙 §6 은 이 수위가 완료 판정의 갈림길이라고 못박는다.

8. **파스텔 톤이 메이플스토리2 느낌인지.** 숫자로 못 잡는다.
   `reports/screenshots/*.png`, 특히 `10_night.png` 를 눈으로 볼 것.
   낮 노출을 낮춰 지면 무늬와 블록 경계가 이제 보인다. 칸별 명도 변주가
   눈에 띄게 드러났는데 (`Palette.VARIATION`, 지금 0.045) 이것이 알맞은
   세기인지 봐 달라.

9. ~~Kenney 팩을 받아 넣을 것~~ — 넣었다. 땅에 까는 것 열두 개다.
   나무·바위·작물 모델도 같은 팩에 있다. 더 쓸지, 어디까지 쓸지 봐 달라.
   `assets/README.md` 에 더 가져오는 법을 적어 두었다.

---

## 명령 모음

```bash
# 전체 테스트
godot --headless --path . -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd -a test -c --ignoreHeadlessMode

# 시각 자가검증 (창이 뜬다)
godot --path . -s res://tools/screenshot_runner.gd

# 게임 실행
godot --path .
```
